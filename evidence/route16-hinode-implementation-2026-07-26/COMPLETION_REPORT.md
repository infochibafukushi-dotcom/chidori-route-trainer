# route-16 日の出線 実装完了レポート

実施日: 2026-07-26 ／ 対象: `route-16`（系統16・日の出線・東京ベイシティ交通）

## 1. 結論

公式（京成バスナビ個別便通過時刻表）で確認できた **2系統** を実装した。全品質ゲートを通過。
コミット・プッシュは行っていない。本番D1へのPUTも行っていない。

| systemKey | 方向 | 起点 → 終点 | 停留所 | path点数 | 距離 | OSM relation |
| --- | --- | --- | ---: | ---: | ---: | ---: |
| `16-hinode-nanachome` | outbound | 新浦安駅 → 日の出七丁目 | 10 | 159 | 2,621 m | 18396563 |
| `16-shinurayasu` | inbound | 日の出七丁目 → 新浦安駅 | 10 | 139 | 2,544 m | 18396562 |

pathHash:
- `16-hinode-nanachome`: `18bed8c3ae9d3e772fa96f9ba737db6257d4911cdbb063de26f8c6b8ec99361a`（resolvedVersion `2026-07-26-hinode-nanachome-v1`）
- `16-shinurayasu`: `9b36bbf512e3c648a82992d8a893049d4a9f52f7e4174ed16b11c904bc1b8de8`（resolvedVersion `2026-07-26-hinode-shinurayasu-v1`）

## 2. 16系統と17系統の切り分け（本件の最重要点）

17系統も「日の出線」を名乗り、日の出七丁目 を発着し、日の出西／順天堂大学・日の出 正門／
プラウド新浦安パークマリーナ／日の出中学校／ベイシティ浦安 を16系統と共有する。
そのため **コース名・中間停留所ののりばだけでは分離できない**。

採用したゲートは二段:

1. **(A) 便が掲載されていた時刻表の凡例**（符号 → 【Ｎ系統】）
2. **(B) その便の出発停留所ののりばの時刻表の凡例** ← 決定打

(B) が【１６系統】に解決し、かつ (A) が16以外に解決していない便のみを採用した。
新浦安駅のりばC = 16系統、日の出七丁目のりば01 = 16系統。17系統は新浦安駅のりば17、符号「ベ／★ベ」。

明示的な排除ガードをコード側にも入れた:

- 17系統OSM relation `18396568 / 18396569 / 18396583` を使用したらビルド・QAが例外で停止する
- 17系統固有停留所（東京電力・日の出東・日の出小学校・日の出保育園入口・アールフォーラム・東口）が
  生成モジュールのデータ領域やランタイム停留所名に現れたら失敗する
- 実測: `_geometry_qa_report.json` の `route17RelationsUsed` は空、ローカル検証の `route17Leak` も両系統で空

既存の route-17 データは一切変更していない（そもそもリポジトリに route-17 モジュールは存在しない）。

## 3. 確認依頼のあった個別項目

| 項目 | 結果 |
| --- | --- |
| 海風の街経由の有無 | 往復とも経由。復路の凡例に「海風の街経由」と明記 |
| 日航浦安（日航東京）方向 | 16系統では経由しない |
| 日の出中学校周辺 | 往復とも停車（ベイシティ浦安↔プラウド新浦安パークマリーナ間） |
| 順天堂大学・日の出正門 | 往復とも停車。「日の出 東口」は17系統のみ |
| 日の出南小学校 | 16系統では通らない |
| 日の出七丁目 折返し | 折返し扱いではなく往復独立relation。往路は降車専用ノード、復路は乗車専用ノードで座標が別 |
| ベイシティ浦安 方向別のりば | 往路 node 6813929333 / 復路 node 1312616418 の別ノード |
| 往復は完全な逆順か | **停留所名の並びは完全逆順**。ただし道路は別relationのため、往路pathの反転による復路生成はしていない |

停留所名の差異: OSM `順天堂大学・日の出正門` vs バスナビ `順天堂大学・日の出 正門`（半角空白）。
正本はバスナビ表記を採用し、突合時のみ空白除去で正規化した。

## 4. 品質ゲート結果

### ジオメトリQA（`_geometry_qa_report.json`, pass: true）

| 項目 | `16-hinode-nanachome` | `16-shinurayasu` |
| --- | --- | --- |
| pathHash 再計算一致 | ✅ | ✅ |
| 最大点間ギャップ（上限30m） | 24.6 m | 24.3 m |
| NaN座標 | 0 | 0 |
| 禁止highway / 建物・緑地通過 | 0 | 0 |
| 一方通行違反 | 0（16区間OK） | 0（13区間OK） |
| 不要なUターン | 0 | 0 |
| のりば↔path 最大距離（上限30m） | 9.1 m | 11.1 m |
| access制限way | 5件すべて `bus=yes` で解決 | 1件、`bus=yes` で解決 |
| 未解決の制限way | 0 | 0 |

### ローカルE2E検証（`_local_validation_report.json`, pass: true）

両系統について以下を確認:

- ランタイム停留所順 == `official-stop-orders.json` 完全一致
- ランタイム pathHash == 同梱バンクのハッシュ、かつ `crypto.subtle` による再計算も一致
- `resolvedVersion` 一致、座標すべて有限、stop id プレフィックス正当
- `path-policy` の `validateRuntimePath` が ok
- 17系統固有停留所の混入なし
- 始発→終点の連続走行が完走、位置ジャンプ 0、10/10 停留所を通過登録

回帰（ローダー変更の影響確認、いずれも正常にロード・解決）:

| route | 停留所 | path点数 |
| --- | ---: | ---: |
| route-15 潮音の街線 | 10 | 163 |
| route-14 弁天・富岡線 | 19 | 299 |
| route-12 舞浜リゾート線 | 21 | 664 |

## 5. 追加・変更ファイル

### 新規（実装）

- `hinode-line-platforms-v1.js`
- `hinode-line-path-v1.js`
- `hinode-line-path-policy-v1.js`
- `hinode-line-stop-images-v1.js`
- `hinode-line-stop-images-v1.css`
- `hinode-line-route-v1.js`

グローバル名は `HINODE_LINE_*`。リポジトリ内に route-17 モジュールが存在しないことを確認済みで衝突なし。

### 変更（配線のみ）

- `service-worker.js`: `CACHE_NAME` を `chidori-route-map-v107` → `v108`、`route-assets-loader.js?v=108`
- `index.html`: `route-assets-loader.js?v=108`
- `route-assets-loader.js`: `PACKS['route-16']` を追加（css 1本 / js 5本、いずれも `?v=108`）

`data.js` は変更なし（`[16,'日の出線']` のスタブが既に存在）。

### 証跡

`official-sources.md` / `official-stop-orders.json` / `system-signatures.json` / `route-pattern-summary.md`
に加え、OSM取得・バスナビスクレイプ・署名ゲート・path生成・ジオメトリQA・ローカル検証の
スクリプトと生データ・レポートを同フォルダに格納した。

## 6. 制約の遵守

- 停留所順・道路形状の推測なし（バスナビ + OSM のみ）
- 「©山本信勝」は未変更（`app.js`）
- 本番D1へのPUTなし、テスト中の編集トークン設定なし
- route-1〜12 / route-14 / route-15 の path・hash・停留所・画像は未変更
- route-17 データは未変更
- 停留所画像の捏造なし（バンクは空で生成）
- コミット・プッシュなし
- 無関係な未追跡 `_*.js` は未変更
